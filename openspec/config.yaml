# SDD Artifact Store Configuration
# Mode: openspec (explicit declaration, not engram/hybrid)
# Generated by sdd-init phase

project:
  name: "notes-app"
  path: "~/code/notes-app"
  type: "spec-driven"

target_stack:
  frontend: "Next.js/React"
  backend: "Django"
  auth: "Django session auth (httpOnly cookie)"
  content: "Markdown note bodies"
  deployment: "same-origin"
  platform: "desktop web"
  viewport: "1280×832"

artifact_store:
  mode: "openspec"
  description: "Open specification artifacts stored as git-committed markdown files"

specs:
  path: "openspec/specs"
  description: "Requirement specifications and design docs"

changes:
  path: "openspec/changes"
  archive: "openspec/changes/archive"
  tracking: "manual and automated change tracking"

requirements:
  version: "v6"
  functional: 27
  non_functional: 8
  data_model: "Django ORM with User, Category, Note models"
  constraints: "Desktop web only, fixed categories, no deletion"

status: "initialized"